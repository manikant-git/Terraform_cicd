name: Terraform Pipeline

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
      # 1) Get your code
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2) Setup Terraform CLI
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      # 3) Configure AWS credentials from GitHub Secrets
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1

      # 4) Init Terraform (downloads providers + connects backend)
      - name: Terraform Init
        run: terraform init

      # 5) Check formatting
      - name: Terraform Fmt
        run: terraform fmt -check

      # 6) Validate configuration syntax
      - name: Terraform Validate
        run: terraform validate

      # 7) Create a plan (preview)
      - name: Terraform Plan
        run: terraform plan -out=tfplan.out

      # 8) Save the plan output as artifact (optional but good)
      - name: Upload Plan
        uses: actions/upload-artifact@v3
        with:
          name: terraform-plan
          path: tfplan.out

